import styled from 'styled-components';

export const RecipeCont = styled.div`
    display: flex;
    width: 100%;
    align-items: top;
    justify-content: space-between;
    position: relative;
    z-index: 0;
    flex-wrap: wrap;
`;
